# Зимний Город - Визуальная Новелла

Браузерная динамичная новелла на чистом HTML/CSS/JS.

## Запуск

Просто открой `index.html` в браузере.

## Функции

- Текст сцены с плавной анимацией
- 2–4 кнопки выбора
- Таймер 6 секунд + прогресс-бар
- Авто-выбор при истечении таймера
- Переменные: stress, money, inventory, flags
- Эффекты выборов (stress, money, предметы, флаги)
- Сохранение в localStorage
- 2 типа концовок + статистика
- Зимний UI с эффектами (мерцание фонаря, снег, шум)

## Структура сцен

Заполни массив `SCENES` в файле `game.js`:

```javascript
const SCENES = [
    {
        id: "start",           // уникальный ID сцены
        text: "Текст сцены...", // текст для отображения
        choices: [             // варианты выбора (2-4)
            {
                text: "Текст кнопки",
                next: "next_scene_id",  // ID следующей сцены
                effects: { ... },       // эффекты (опционально)
                condition: { ... },     // условие показа (опционально)
                hesitate: true          // вариант "замешкался" (опционально)
            }
        ]
    },
    // Сцена финала
    {
        id: "ending_good",
        ending: true,
        title: "ПОБЕДА",
        text: "Текст финала..."
    }
];
```

## Эффекты (effects)

```javascript
effects: {
    stress: 10,           // +10 к стрессу (или -10)
    money: -100,          // -100 денег (или +100)
    addItem: "🔑",        // добавить предмет в инвентарь
    removeItem: "📱",     // убрать предмет
    setFlag: "metBoss",   // установить флаг
    removeFlag: "hasKey"  // убрать флаг
}
```

## Условия (condition)

```javascript
condition: {
    flag: "hasKey",        // показать если есть флаг
    noFlag: "isDead",      // показать если НЕТ флага
    hasItem: "🔑",         // показать если есть предмет
    minMoney: 500,         // показать если денег >= 500
    maxStress: 50          // показать если стресс <= 50
}
```

## Специальные сцены

Если `stress >= 100` → переход на `ending_stress`
Если `money < 0` → переход на `ending_money`

## Предметы

Добавь иконки в объект `ITEMS`:

```javascript
const ITEMS = {
    "🔑": "Ключ",
    "📱": "Телефон",
    "💊": "Таблетки"
};
```

## Пример минимальной истории

```javascript
const SCENES = [
    {
        id: "start",
        text: "Ты стоишь на холодной улице. Только что тебя уволили...",
        choices: [
            { text: "Пойти домой", next: "home", effects: { stress: 5 } },
            { text: "Зайти в бар", next: "bar", effects: { money: -200, stress: -10 } },
            { text: "Стоять и думать", next: "think", hesitate: true, effects: { stress: 15 } }
        ]
    },
    {
        id: "home",
        text: "Дома холодно. Нужно заплатить за отопление.",
        choices: [
            { text: "Заплатить (500₽)", next: "warm", effects: { money: -500 }, condition: { minMoney: 500 } },
            { text: "Терпеть холод", next: "cold", effects: { stress: 20 } }
        ]
    },
    // ... больше сцен
    {
        id: "ending_good",
        ending: true,
        title: "ВЫЖИЛ",
        text: "Тебе удалось продержаться. Весна близко..."
    },
    {
        id: "ending_stress",
        ending: true,
        title: "СЛОМАЛСЯ",
        text: "Стресс оказался слишком сильным..."
    },
    {
        id: "ending_money",
        ending: true,
        title: "БАНКРОТ",
        text: "Деньги закончились..."
    }
];
```

## Файлы

```
novel-game/
├── index.html   # HTML структура
├── style.css    # Стили + эффекты
├── game.js      # Логика + SCENES
└── README.md    # Документация
```
